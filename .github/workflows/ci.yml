name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e '.[dev,risk,serve,otlp,vault,aws]'
      - name: Format check
        run: ruff format --check underwrite/ tests/
      - name: Lint
        run: ruff check underwrite/ tests/
      - name: Type check
        run: mypy underwrite/ tests/
      - name: Security audit
        run: bandit -r underwrite/ -c pyproject.toml
      - name: SPDX header presence
        run: |
          set -euo pipefail
          missing=$(find underwrite tests -name '*.py' -not -name '__version__.py' \
                    -exec sh -c 'head -1 "$1" | grep -q "SPDX-License-Identifier" || echo "$1"' _ {} \;)
          if [[ -n "${missing}" ]]; then
            echo "Files missing SPDX-License-Identifier header:"
            echo "${missing}"
            exit 1
          fi
      - name: Dependency audit
        run: pip-audit --skip-editable
      - name: Test
        run: python -m pytest tests/ -v --tb=short --cov=underwrite --cov-report=term --cov-fail-under=80

  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v7
      - name: Build Docker image
        run: |
          docker build \
              --build-arg "PYTHON_VERSION=${{ matrix.python-version }}" \
              --build-arg "BUILD_VERSION=ci-${GITHUB_SHA::12}" \
              --build-arg "GIT_COMMIT=${GITHUB_SHA::12}" \
              --build-arg "BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
              --tag "underwrite:ci-${{ matrix.python-version }}" \
              --provenance=false \
              --sbom=false \
              -f Dockerfile .
      - name: Smoke test
        run: |
          docker run -d --name "underwrite-ci-${{ matrix.python-version }}" \
              -p 8080:8080 \
              -e UNDERWRITE_REQUIRE_AUTH=false \
              -e UNDERWRITE_DATA_DIR=/tmp/uw-data \
              "underwrite:ci-${{ matrix.python-version }}"
          sleep 5
          curl -sf http://localhost:8080/healthz || { docker logs "underwrite-ci-${{ matrix.python-version }}"; exit 1; }
          curl -sf -X POST http://localhost:8080/v1/health || exit 1
          docker stop "underwrite-ci-${{ matrix.python-version }}"

  secrets-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: TruffleHog secret scan
        uses: trufflesecurity/trufflehog@3a67e6c3f2b56e1d8a8f1c6b9c4e9a4f8d9b6e8b # pinned commit
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
